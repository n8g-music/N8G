{
  "name": "n8g-creative-os",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "sync": "bun run drive-sync/sync-main.ts",
    "sync:watch": "bun run drive-sync/watcher.ts",
    "sync:dry": "bun run drive-sync/sync-main.ts --dry-run",
    "sync:watcher": "./scripts/start-git-watcher.sh"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
